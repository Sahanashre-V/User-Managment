meta {
  name: Login
  type: http
  seq: 4
}

post {
  url: http://localhost:8888/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email":"admin@test.com",
    "password":"admin123"
  }
}

tests {
  const body = res.body;
  bru.setEnvVar("authToken", body.token);
  
}
